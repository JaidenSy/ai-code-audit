{
  "name": "ai-code-audit",
  "version": "0.1.0",
  "description": "GitHub Action to audit AI-generated code for security, compliance, and quality issues",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build dist/index.js -o dist --source-map --license licenses.txt",
    "build:action": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "jest",
    "test:local": "ts-node test/run-local-test.ts",
    "lint": "eslint src/**/*.ts",
    "start": "node dist/index.js"
  },
  "keywords": [
    "github-action",
    "ai",
    "code-review",
    "security",
    "compliance",
    "copilot",
    "cursor"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/ncc": "^0.38.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  }
}
